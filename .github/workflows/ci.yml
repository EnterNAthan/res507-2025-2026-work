name: CI Preview
# ci preview ok 
on:
  push:

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run preview script
        run: ./scripts/ci-preview.sh